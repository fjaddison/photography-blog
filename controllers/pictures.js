const express = require('express')
// assign schema (defibned in schema.js) to variable
const News = require('../models/schema') // file extention not required

const router = express.Router()

module.exports = router
